def Linear_search(arr,target):
    for i in range(len(arr)):
        if (arr[i]==target):
            return i
        return "not found"
arr=[2,5,1,4,6,7,8,9]
target=1
print(Linear_search(arr,target))
